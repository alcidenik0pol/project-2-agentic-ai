services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8901:8901"
    env_file:
      - .env
    environment:
      - CORS_ORIGINS=http://localhost:3456,http://127.0.0.1:3456,http://frontend:3456
    volumes:
      - ./output:/app/output
      - ./docs/credentials/agenticaicolumbia-72b6c0b1b975.json:/app/docs/credentials/credentials.json:ro

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3456:3456"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8901
      - PORT=3456
    depends_on:
      - backend
