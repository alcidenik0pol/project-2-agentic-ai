# Backend API dependencies
# Pin BOTH fastapi AND starlette. fastapi==0.135.3 declares
# `starlette>=0.46.0` with no upper bound, so a fresh pip resolve pulls
# starlette 1.3.1 — whose BaseHTTPMiddleware silently strips CORS headers
# added by CORSMiddleware when another @app.middleware("http") is layered
# on top (our usage-limit check). Result: zero access-control-* headers,
# OPTIONS → 405. Pinning starlette==0.52.1 matches the verified-working
# local conda env. Do not unpin either without re-verifying CORS end-to-end.
fastapi==0.135.3
starlette==0.52.1
uvicorn[standard]>=0.27.0
websockets>=12.0
python-dotenv>=1.0.0

# Agent pipeline dependencies (inherited from root requirements.txt)
praw>=7.7.0
pydantic>=2.0.0
openai>=1.0.0
google-cloud-aiplatform>=1.38.0
scikit-learn>=1.3.0
numpy>=1.24.0
requests[socks]>=2.31.0
langgraph>=1.0.0
langchain-google-genai>=2.0.0
langchain-core>=1.0.0

# Data source dependencies (must be here — Dockerfile only installs this file)
duckdb>=1.0.0          # pushshift Parquet queries
pyarrow>=15.0.0        # DuckDB Parquet backend
beautifulsoup4>=4.12.0 # reddit_v2 HTML parser
lxml>=5.0.0            # BeautifulSoup parser backend
